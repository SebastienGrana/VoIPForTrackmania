node_modules/
.env
*.env.local
*.log

# Build output: OnZVoIP.op is rebuilt by tools/build-op.ps1, and the Discord
# announcement next to it is event-specific rather than part of the project.
dist/
openplanet pluggin folder.txt
openplanet plugginfolder.txt
context.txt
todo.txt

# Local dev overrides: may contain a real TCP_SHARED_SECRET, never commit
docker-compose.override.yml
todo.html

# Maquette de référence et sa spec : décision 2026-08-10, restent en local.
design/

# Archives de travail et notes locales balayées par erreur dans 83d4d7f
# (logadmin.txt contenait les identifiants /admin en clair — mot de passe
# rotaté le 2026-08-14). Rien de tout ceci n'appartient au dépôt.
OnZVoIP.rar
OnZVoIP.zip
OnZVoIPv3.zip
logadmin.txt
.claudeignore
